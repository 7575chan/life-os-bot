# 人生管理OS Discord BOT 仕様書（改訂版）

出典:
- `2026-09-19 18-06人生管理OS.md`（私仕様・優先）
- `…人生管理OSの作り方完全マニュアル.txt`（原案）
- `執筆記録.xlsx`（執筆管理シートの実物。Google スプレッドシート版と同じ構造）
- ユーザーからの追加指示（Obsidianアクセス制限、タスク双方向同期、Python導入済み）

矛盾する場合は私仕様を採用する。原案との主な差分は「14チャンネル構成」「スプレッドシート × Obsidian のハイブリッド保存」「部屋ごとのAI応答モード」。

## 0. この改訂での主な変更点

| # | 変更 | 節 |
|---|---|---|
| 1 | Obsidian の書き込み系操作を許可リスト方式に限定（読み取りは Vault 全体で許可） | §2 |
| 2 | Obsidian の保存先を `06-Life-OS/` 配下の各部屋フォルダに変更 | §3 |
| 3（`09-idea` は完了。`08-scrap` `10〜12` はこれから） | 執筆記録シートの実構造を反映（作品ごとの列・目標・締切・日次スナップショット） | §4 |
| 4 | Obsidian の `01 Life-OS-Task` とスプレッドシートのタスクを双方向同期 | §6 |
| 5（GCP 常時稼働の部分は準備完了・ユーザー作業待ち） | タスクシートに `ID` `期限` 列を追加、`優先度` を採用 | §3.2 |
| 6 | 開発環境は Python 3.14.7 導入済み。ライブラリ対応を最初に検証 | §1 |
| 7 | 稼働環境は GCP VM、Obsidian は Drive API v3（`drive` 方式）で確定。認証は**サービスアカウント（credentials.json）のみ**。OAuth は採用しない（同意画面・7日失効の問題が発生しない構造） | §1・§2.4 |
| 8 | ガードの要件を明文化（許可外の書き込みは拒否・エラーログ記録・安全に中断） | §2.3 |
| 9 | 確認事項への回答を反映し、未確定事項を更新 | §10 |
| 10 | 読み取りは許可に変更（許可外ノートも参照可）。Drive の起点を Vault ルートに変更 | §2・§2.4 |
| 11 | 権限設定が完了（Vault=閲覧者、`06-Life-OS`・タスク棚=編集者）。Vault ルート ID を受領し、セキュリティ実装（ガード・テスト・CLAUDE.md）を反映 | §2.4・§8.1 |
| 12 | 実機検証を実施。権限は設定どおり動作。**サービスアカウントは新規ファイルへ内容を書けない**ことが判明 | §2.4 |
| 13 | 新規ファイル作成は **GAS 中継方式（B）を採用**。中継の設計（API・セキュリティ・デプロイ・テスト）を追加 | §2.5・§9 |
| 14 | 空ファイル4つの更新を実機で確認。実際のファイル名（`Health-Manual.md`・`CEO-Directives.md`）に仕様を合わせた | §2.4・§3.1 |
| 15 | **シート名をチャンネル名に統一**。ユーザーが作成済みの14個のシートをそのまま使う。`10-project-novel` `11-project-trpg` `12-project-others` は別々のシートに分ける | §3.2・§5 |

## 1. 基本方針

| 項目 | 内容 |
|---|---|
| 言語 / ライブラリ | Python、discord.py、anthropic、gspread、google-api-python-client、httpx、trafilatura |
| Python | 3.14.7 導入済み。着手時に依存ライブラリ（特に trafilatura/lxml）の対応を検証し、未対応なら 3.12 の venv を使う |
| 動作環境 | 常時稼働は GCP VM（24時間、systemd）。開発はローカル。タイムゾーンは `Asia/Tokyo` 固定 |
| Obsidian 接続 | Google Drive API v3（`drive` 方式）で確定。開発時のみ `local`（同期フォルダ）も選べる |
| 秘密情報 | `.env` と `credentials.json`。どちらも `.gitignore`。読み書きは Bot のコード以外から行わない |
| 日付形式 | `YYYY-MM-DD`、時刻は `HH:MM`。読み込み時は `/` 区切りも許容 |
| Claudeモデル | 既定 `claude-sonnet-5`（`CLAUDE_MODEL` で変更可）。画像認識も同モデル |
| 人格（全部屋共通） | 全肯定・黒衣（裏方）の秘書。正論・説教・お節介は禁止。「1文字でも進んだら100点」。減点評価をしない。未完了を責めない |

## 2. Obsidian アクセス制限（最重要・厳格ルール）

手動整理している他のノートやフォルダを誤って破壊・変更しないよう、**書き込み系の操作は許可リストのパスに限る**。読み取りは Vault 全体で許可する（ユーザー指示。以前の「読み取りも禁止」は撤回）。

| 操作 | 範囲 |
|---|---|
| 読み取り（read / list / search） | Vault 内のすべてのファイル・フォルダ（Vault の外は不可） |
| 書き込み・変更・新規作成・改名・削除 | 下記の許可リストのみ。それ以外は拒否 |

### 2.1 書き込みを許可する範囲（許可リスト）

| # | 対象 | 備考 |
|---|---|---|
| 1 | `06-Life-OS/` 配下のすべてのファイル・フォルダ | `01-today-task` 〜 `14-ai` の各部屋フォルダ・ノート |
| 2 | `01🗃Task/01 Life-OS-Task.md` ファイルのみ | タスク棚。§6 の同期対象。**内容の書き換えのみ**（改名・削除・別名での作成は禁止）。ファイル名は `OBSIDIAN_TASK_FILE` で明示 |

### 2.2 書き込み禁止（読み取りのみ可）

手動管理している既存の重要データ。Bot は読めるが、**変更・上書き・削除・改名・新規作成は一切しない**。

- `01🗃Task/Project/` 配下のすべてのノート
- `01🗃Task/Archive/` 配下のすべてのノート
- `01🗃Task/` 内の `01 Life-OS-Task.md` 以外のすべてのファイル
- その他の既存フォルダすべて（`00inbox`、`02 📝 memo`、`03-Writing`、`90💻project` など）
- 上記に列挙がなくても、許可リストにないパスへの書き込み系操作はすべて拒否（**拒否がデフォルト**）

### 2.3 実装要件

1. `notes.py` の全操作の入口で `check(path, 操作種別)` を呼ぶ。書き込み系（write / rename / delete）が許可外なら `AccessDenied` を送出し、処理を進めない。read / list は Vault 内であれば通す。
2. パス判定は正規化してから行う: `..` と絶対パスを拒否、区切りを `/` に統一、Unicode を NFC に統一（絵文字入りフォルダ名 `🗃` `📝` の表記ゆれ対策）、大文字小文字は区別して比較。読み取りでも Vault の外に出るパスは拒否する。
3. `local` バックエンドでは `resolve()` 後のパスが Vault 内であること、および**シンボリックリンクで許可領域の外へ書き込まない**ことを確認する（判定は解決後のパスで再度行う）。
4. `rename` は移動元・移動先の**両方**が書き込み許可内であること（許可外のノートを許可領域へ移動＝元ファイルの変更になるため禁止）。
5. 削除は完全削除ではなく、Drive のゴミ箱または `06-Life-OS/.trash/` への退避にする。
6. 拒否は `data/guard.log` に記録する（§2.4）。
7. `14-ai` の tool_use（`search_notes` / `read_note` / `append_note`）も同じガードを通る。ツール側で個別に判定を持たない。`append_note` は許可領域内のみ。
8. 許可リストは `notes_policy.py` の定数として一箇所に置き、`tests/test_notes_policy.py` で以下を検証する:
   - 書き込み許可パスの通過
   - 許可外（`01🗃Task/Project/x.md`、`00inbox/x.md`、`01🗃Task/` 直下の別ファイル）への write / rename / delete の拒否
   - `06-Life-OS/../00inbox/x.md` などの回避の拒否、前方一致すり抜け（`06-Life-OS-evil/`）の拒否
   - NFC/NFD 表記違い、大文字小文字違いの扱い
   - 許可外ファイルの**読み取りは成功し、内容が変化しない**こと
   - 許可外から許可領域への rename の拒否、タスク棚の rename/delete の拒否
   - シンボリックリンク経由の許可外への書き込みの拒否
   - 拒否時に、バックエンドの書き込み API が呼ばれないこと
9. この制約は `CLAUDE.md` にも仕様として記載する（実装着手時に反映する。§10 参照）。

### 2.4 拒否時の挙動と Drive 接続

**許可外への書き込み系の処理が発生した場合の挙動**（必須）:
1. 該当操作を**実行せずに拒否**する（バックエンドの API を呼ぶ前に判定する。書き込み前に止まるため、ロールバックは不要）
2. エラーログを記録する: `data/guard.log` に「日時・操作種別（write/rename/delete）・対象パス・理由」を ERROR レベルで残す
3. 例外 `AccessDenied` で**安全に処理を中断**する。中途半端な状態を残さないよう、Obsidian への書き込みを先に行い、失敗したらスプレッドシート側への記録を行わない順序にする
4. Discord には「そのファイルは触らない設定になっています」とだけ返す（詳細は返さない）

**Drive 接続（`drive` 方式・サービスアカウントのみ）**:
- 認証は `credentials.json`（サービスアカウント）だけを使う。OAuth のコード・`token.json`・同意画面の設定は一切持たない。トークンの期限切れ（7日失効）は発生しない（サービスアカウントの鍵で都度アクセストークンを取得するため）
- Google Drive とスプレッドシートは、サービスアカウントのメールアドレスに**編集者**として共有して運用する（ユーザー指示）:
  - Obsidian の Vault フォルダ（または少なくとも `06-Life-OS` フォルダと `01 Life-OS-Task.md`）
  - Bot 用スプレッドシート（`GOOGLE_SHEET_ID`）。執筆記録シートは閲覧者で共有済み
- 接続先の ID と共有権限（ユーザー提供・**権限設定は完了済み**）:

  | 対象 | ID | 環境変数 | 共有権限 |
  |---|---|---|---|
  | Vault ルートフォルダ | `1E1-2sFEyjhhMD5dKbXtPBMZ1VQS5agX6` | `OBSIDIAN_VAULT_FOLDER_ID` | 閲覧者（読み取り・検索用） |
  | `06-Life-OS` フォルダ | `1vULPktHtQKTeg6lp81ZVldXTi5rQaiDM` | `OBSIDIAN_LIFEOS_FOLDER_ID` | 編集者（書き込み用） |
  | `01 Life-OS-Task.md` | `1DNw8aioI_QA-xlE15FMwq41ZiXbvr9Ja` | `OBSIDIAN_TASKFILE_ID` | 編集者（書き込み用） |

  Vault ルートが閲覧者のため、Drive 側の権限でも許可外ノートへの書き込みは防げる（コード側のガードとの二重防御）。ID は秘密情報ではないが、`.env.example` に記載済み。
- パスの解決は ID を起点に行う: `06-Life-OS/…` は `OBSIDIAN_LIFEOS_FOLDER_ID` から辿り、タスク棚は `OBSIDIAN_TASKFILE_ID` で直接読み書きする。許可外ノートの**読み取り**（§2 の表）は `OBSIDIAN_VAULT_FOLDER_ID` を起点にする。未設定の場合は、許可外ノートの読み取りは「未設定」として失敗させる（書き込みの安全性には影響しない）
- タスク棚は「既存ファイルの中身の更新」のみ。改名・削除・別名での新規作成は禁止
- **実機検証の結果（2026-09-19・Drive API 有効化後）**:

  | 項目 | 結果 |
  |---|---|
  | 権限（Vault ルート） | 閲覧のみ（`canEdit=False`）。許可外への書き込みは Drive 側でも不可 |
  | 権限（`06-Life-OS`・タスク棚） | 編集可（`canEdit=True`） |
  | タスク棚の読み取り | 成功 |
  | 既存ファイル（ユーザー所有）の更新 | 成功（タスク棚に同一内容を書き戻して確認。内容は変化なし） |
  | 空ファイル・フォルダの新規作成 | 成功（サービスアカウント所有になる。検証用はゴミ箱へ移動済み） |
  | **中身のあるファイルの新規作成** | **失敗**: `403 Service Accounts do not have storage quota` |
  | **サービスアカウント所有ファイルへの内容書き込み** | **失敗**（同じ容量エラー。空ファイルを先に作る方法も不可） |
  | ユーザー作成の空ファイル4つ（`Ideas.md` `Inbox.md` `CEO-Directives.md` `Health-Manual.md`）への中身のある更新 | **成功**（日本語・絵文字を含む本文を書き込み→読み戻しで一致→元の内容に復元） |
  | Vault 全体の検索（読み取り） | 成功 |
  | `06-Life-OS` の部屋フォルダ（`01-today-task`〜`14-ai` の14個） | すべて存在 |
  | 許可外パスへの書き込み（ガード） | 拒否（実機の接続設定のもとでも確認） |

  **結論**: サービスアカウント単独では、**Bot が新しいノートを作って中身を書くことはできない**（サービスアカウントは保存容量を持たないため）。できるのは、①**ユーザー所有の既存ファイル**の内容の更新、②フォルダの作成、③空ファイルの作成（中身は書けない）。OAuth へは勝手に切り替えない。ユーザー所有ファイルへの「中身のある」更新は、所有者の容量が使われるため成功することを、ユーザーが作成した4ファイルで確認した。

- **新規ファイルが必要なノート**（この制約の影響を受け、GAS 中継（§2.5）で作成するもの）: 日記 `03-looking-back/YYYY-MM-DD.md`（毎日）、スクラップ `08-scrap/<日付>_<タイトル>.md` と画像（記事ごと）、プロジェクトノート `Novel_<作品名>.md` 等（作品ごと。「未命名ファイルの自動作成」「改名」を含む）、レポート `04-report/Weekly_*.md` `Monthly_*.md`。影響を受けない（ユーザーが作成済みで、更新だけで済む）もの: `Ideas.md`、`Inbox.md`、`CEO-Directives.md`、`Health-Manual.md`、タスク棚
- **採用方針**: 選択肢 B（Google Apps Script による中継）を採用した。詳細は §2.5。（A: ユーザーが事前に作成／C: 共有ドライブ は不採用）
- **プライバシーの注意**: 許可外のノートを読んで回答に使うと、その内容は Claude API に送信される。定期処理（朝報告・レポート等）では許可外ノートを読まず、ユーザーが依頼した検索・相談のときだけ読む

### 2.5 新規ファイルの作成（GAS 中継方式・採用）

**決定（2026-09-19）**: 選択肢 B を採用する。手作業を無くし、日次日記・記事別スクラップ・作品別ノートの自動生成という当初の仕様を維持する。

**役割分担**

| 操作 | 実行者 | 備考 |
|---|---|---|
| 読み取り・一覧・検索 | Bot（サービスアカウント） | 直接 Drive API |
| 既存ファイルの内容の更新 | Bot（サービスアカウント） | ユーザー所有ファイルの更新は実機で確認済み（4ファイル） |
| 新規ファイルの作成（中身付き）・画像などの作成 | **GAS 中継（ユーザー権限）** | サービスアカウントは容量が無く不可 |
| フォルダの作成・改名・ゴミ箱への移動 | **GAS 中継（ユーザー権限）** | 所有者をユーザーに統一するため。サービスアカウントでの改名・削除の可否は未検証なので、確実な経路に統一する |

**書き込みの流れ**: `GuardedStore.write(path, text)` → ポリシー判定（§2.3。許可外はここで拒否）→ `DriveStore` が対象ファイルの有無を確認 → **存在すればサービスアカウントが更新**、**存在しなければ GAS 中継で作成**（親フォルダが無ければ中継が作成）。呼び出し側は、新規か更新かを意識しない。作成後のファイルはユーザー所有になるため、以降の更新はサービスアカウントで行える（初回のデプロイ時に実機で確認する）。

**中継 API（Bot → GAS）**: `POST` JSON。応答も JSON（`{ok, status, id, url, message}`）。

| op | 内容 | 制約 |
|---|---|---|
| `create_file` | パスと本文（UTF-8 テキスト）で新規作成 | 既存なら作成せず `status=exists`（**上書きしない**） |
| `create_bytes` | 画像などバイナリ（`content_base64`）を新規作成 | 上限 5MB。既存なら `exists` |
| `rename` | 改名・移動 | 移動元・移動先の両方が許可領域内 |
| `trash` | ゴミ箱へ移動（復元可能） | ファイルのみ。フォルダ・`06-Life-OS` 自体・部屋フォルダは不可 |

リクエストには `token`、`op`、`path`（Vault 相対）、`request_id`（再送しても重複作成されない）を含める。**読み取り・更新・一覧の op は持たない**（Bot が直接行うため、中継の権限を最小にする）。

**セキュリティ**（中継は「ユーザー権限で Drive に書ける入口」になるため、二重・三重に制限する）:
1. **デプロイ設定**: 実行ユーザー=自分、アクセス=全員（匿名可）。Bot は Google ログインなしで呼ぶため必要。URL は非公開とし、加えて次のトークンで認証する
2. **トークン**: 32バイト以上のランダム値。GAS の Script Properties（`RELAY_TOKEN`）と `.env`（`GAS_RELAY_TOKEN`）にだけ保存。コード・ログ・リポジトリに出さない。ローテーション手順を `gas/README.md` に記載。GAS 側は定数時間で比較する
3. **許可領域の再検証（GAS 側）**: 起点は Script Properties の `LIFEOS_FOLDER_ID`（`06-Life-OS` フォルダ）。パスは `06-Life-OS/` で始まること、`..` `.` 空区切り・バックスラッシュ・制御文字を拒否、Unicode は NFC。それ以外は `status=denied` を返し実行しない。**タスク棚や Vault のその他の場所は中継の対象外**（触れない）
4. **Bot 側のガード**: 中継を呼ぶ前に必ず §2.3 のポリシー判定を通す。`denied` 応答は `data/guard.log` にも ERROR で記録し、`AccessDenied` として扱う
5. **同時実行**: `LockService` で作成・改名を直列化。`request_id` を `CacheService` に6時間保持し、同じ ID の再送には前回の結果を返す

**Bot 側の実装方針**: `gas_relay.py`（httpx、タイムアウト30秒、リダイレクト追従（GAS は 302 で別ホストへ転送する）、失敗時は同じ `request_id` で最大2回再試行）。`DriveStore` の新規作成・改名・削除・フォルダ作成がこれを使う。中継が未設定または失敗した場合は `RelayError`。呼び出し元は「うまく記録できませんでした」と返し、Obsidian への書き込みを先に行う順序（§2.4）を保つ（失敗時にシートへは記録しない）。

**リポジトリ構成**: `gas/Code.gs`（中継本体。パス検証は純関数）、`gas/appsscript.json`（`webapp`: `executeAs=USER_DEPLOYING`、`access=ANYONE_ANONYMOUS`、`oauthScopes`: `drive`）、`gas/README.md`（デプロイ手順）、`scripts/verify_relay.py`（実機検証）。

**デプロイ（ユーザーが行う作業）**: ①`script.google.com` で新規プロジェクトを作成 ②`Code.gs` と `appsscript.json` を貼り付け ③Script Properties に `RELAY_TOKEN` と `LIFEOS_FOLDER_ID` を設定 ④エディタで一度実行して Drive の権限を**スクリプト所有者本人が承認**（自分のスクリプトなので「未確認のアプリ」の警告が出たら詳細から続行）⑤ウェブアプリとしてデプロイし、URL を `.env` の `GAS_RELAY_URL` に、トークンを `GAS_RELAY_TOKEN` に設定 ⑥`scripts/verify_relay.py` で動作確認。この承認はスクリプト所有者のもので、Bot は Google のトークンを保持しない。OAuth 同意画面の設定や7日での失効は発生しない（承認を取り消さない限り有効）。コード更新時は同じデプロイを更新して URL を変えない。

**テスト方針**: Bot 側は `httpx.MockTransport` の偽中継で、①ファイルが無ければ中継で作成、②有れば中継を呼ばずサービスアカウントで更新、③許可外パスは中継を呼ぶ前に拒否、④`denied` 応答の扱い、⑤再送時に同じ `request_id`、⑥トークンがログに出ない、を検証する。GAS 側は自動テスト環境が無いため、`verify_relay.py` で実機検証する（作成→更新→改名→ゴミ箱、許可外パスの拒否、トークン不一致の拒否、既存ファイルの上書き拒否）。

**制限と注意**: GAS の実行は1回あたり1〜3秒かかることがある（新規作成時のみ）。個人利用の範囲では GAS の日次クォータに収まる見込み。中継を無効にしたい場合は、Script Properties の `RELAY_TOKEN` を変更（または削除）すれば即座に止まる。

## 3. 保存構造（ハイブリッドDB）

- **Googleスプレッドシート**: 数値・日付・フラグ（タスク、体調、日記、家計簿、帳簿、記事一覧など）。ほかに、参照専用で執筆記録シート（§4）
- **Obsidian（`.md`）**: 長文・全文・プロジェクト・方針。書き込みは §2 の許可領域のみ（読み取りは Vault 全体）
- **状態ストア（SQLite `data/state.db`）**: Botメッセージ ID と処理文脈の対応、定時ジョブの実行済み判定、タスク同期のスナップショット

### 3.1 Obsidian のフォルダ構成（許可領域内）

```
06-Life-OS/
  01-today-task/      （朝報告のログ用。必要になるまで空でもよい）
  02-health/          Health-Manual.md
  03-looking-back/    YYYY-MM-DD.md（日記）
  04-report/          Weekly_YYYY-MM-DD.md / Monthly_YYYY-MM.md
  05-private/         Inbox.md
  06-household-accounts/
  07-ledger/
  08-scrap/           <日付>_<タイトル>.md、attachments/
  09-idea/            Ideas.md
  10-project-novel/   Novel_<作品名>.md
  11-project-trpg/    TRPG_<作品名>.md
  12-project-others/  Project_<名前>.md
  13-im-the-ceo/      CEO-Directives.md
  14-ai/              （必要に応じて作業メモ）
01🗃Task/01 Life-OS-Task.md    ← タスク棚（§6）
```

`06-Life-OS/` 直下の部屋フォルダ（14個）は作成済み。不足したフォルダは GAS 中継（§2.5）が作成する。ユーザーが作成した既存ファイルの名前は `Ideas.md` `Inbox.md` `CEO-Directives.md` `Health-Manual.md`（ハイフン区切り）で、仕様もこれに合わせる。`06-Life-OS/` 自体が存在しない場合は作成せず、起動時に警告して停止する（Vault の場所を誤っている可能性があるため）。

### 3.2 スプレッドシートのシート構成（Bot 用ブック `GOOGLE_SHEET_ID`）

**方針（ユーザー指示）**: シート名は Discord のチャンネル名と**同じ**にする（`CHANNEL_*` の値と完全一致）。ユーザーが作成済みの14個のシートをそのまま使い、機能名（「タスク」「体調」など）の別名のシートは作らない。`10-project-novel` `11-project-trpg` `12-project-others` は、あとで見返すときに量が多くなって確認しにくくなるため、**別々のシート**にする。

| シート名（=チャンネル名） | 役割 | 列 |
|---|---|---|
| `01-today-task` | タスク（バックログを含む全タスク） | ID / 登録日 / 内容 / 完了 / 実行予定日 / 期限 / 優先度（高・中・低・空） / 出典 / 完了日 |
| `02-health` | 体調 | 日時 / 睡眠時間 / 歩数・運動 / 客観指標 / 主観メモ / AI判定 / 体調スコア(1-5) |
| `03-looking-back` | 日記 | 日付 / ご機嫌度 / 天気 / 本文（整形後） / Obsidianリンク |
| `04-report` | 発行したレポート | 発行日 / 種別 / 本文 |
| `05-private` | 私生活メモ | 日時 / 内容 / タグ |
| `06-household-accounts` | 家計簿 | 日付 / 金額（円） / ジャンル / 備考 / 区分（現金・娯楽） |
| `07-ledger` | 事業の帳簿 | 日付 / 種別（売上・経費） / 金額（円） / 内容 / やよい転記 / 分析シート転記 |
| `08-scrap` | 保存した記事 | 日時 / タイトル / URL / 3行要約 / タグ / Obsidianリンク |
| `09-idea` | アイデア | 日時 / タグ / 内容 |
| `10-project-novel` | 小説プロジェクトの記録 | 日時 / 作品名 / 種別 / 内容 |
| `11-project-trpg` | TRPG プロジェクトの記録 | 日時 / 作品名 / 種別 / 内容 |
| `12-project-others` | その他プロジェクトの記録 | 日時 / 作品名 / 種別 / 内容 |
| `13-im-the-ceo` | 方針・宣言（`CEO-Directives.md` と同期） | 日時 / 内容 |
| `14-ai` | 操作ログ（`14-ai` でのデータ変更・削除の記録） | 日時 / 操作 / シート / 行 / 変更前 / 変更後 |

**ルール**
1. **名前の一致**: シート名は `config.CHANNELS` の値から決める。チャンネル名を変える場合は、シート名も同じに変える（自動では改名しない）。操作ログの「シート」列にも、このシート名（=チャンネル名）を記録する。
2. **既存シートを使う**: ユーザーが作成済みの14個の空のシートに、見出し行（上の「列」）を書き込んで使う。新しいタブは、必要になるまで作らない。存在しないシートがある場合のみ、同じ名前で作成する。
3. **見出し行の扱い**: 1行目が空なら見出しを書く。`08-scrap` のように、A1 に**シート名と同じ文字列だけ**が入っている場合（タイトル代わり）は、見出し行に置き換える。それ以外の内容が入っているシートには**書き込まず**、起動時に警告して該当機能を止める（ユーザーのデータを上書きしない）。
4. **見出しが既にある場合**: 列は位置ではなく列名で読み書きする（順序違いは許容）。必要な列が足りないときだけ、末尾に列を追加する。余分な列は残す。
5. **チャンネル外のシート**: チャンネル名に対応しないシートには一切触れない。

## 4. 執筆記録シート（参照専用・文字数の入力元）

`執筆記録.xlsx` と同じ構造の Google スプレッドシートを、Bot は**読み取りだけ**する（書き込まない）。ブックは `WORDCOUNT_SHEET_ID`、タブは `WORDCOUNT_TAB`（既定 `シート1`）。サービスアカウントに閲覧権限で共有する。

### 4.1 実物の構造（`シート1`）

| 行 | 内容 |
|---|---|
| 1行目 | `A1` はアカウント情報（無視）。`B1` 以降が**作品名**（現在24作品）。`YYYY-MM ` の接頭辞が付くものがある |
| 2行目 | `A2`=「目標」。各作品の目標文字数 |
| 3行目 | `A3`=「締切」。各作品の締切（日付のシリアル値） |
| 4行目 | `A4`=「単位」。値は1 |
| 5行目 | `A5`=「Google Doc ID」。各作品の元ドキュメント（Bot は使わない） |
| 6行目〜 | `A列`=記録日時（シリアル値、毎日 09:45 前後に1行）、`B列〜`=その時点の**各作品の総文字数**（累計） |

実データは 2026-08-31 〜 2026-09-19 の20行（1日1行）。同ブックには旧来の作品別管理タブ（「〜の執筆管理シート」「シート2」等）と `シート1 のコピー` もあるが、これらは**使わない**。

### 4.2 読み取りルール

1. 作品の特定は**列名（1行目の作品名）で行う**。`シート1 のコピー` のように作品の並びが違うタブがあるため、列位置に依存しない。作品名は先頭の `YYYY-MM ` を除いた文字列を正規名とする。
2. セルは `UNFORMATTED_VALUE` で取得し、日時はシリアル値（1899-12-30 起点）から JST の日時に変換する。書式が文字列日付でも解釈できるよう `parse_date_any` にフォールバックする。
3. 各データ行の `A列` の日付で行を特定する。空セル・0 は「未記録/0文字」として扱い、エラーにしない。
4. **日次の増加**: 対象日の行の総文字数 − 直前の記録行の総文字数（作品ごと）。全作品の合計も出す。
5. **マイナス（削除・推敲）**は「マイナス」と表示せず、「推敲」として増加に含めない（合計の増加は0以上に丸める）。減点しない方針に合わせる。
6. **アクティブ作品**: 直近14日に総文字数が増加した作品。朝報告・週次では原則アクティブ作品のみ表示する。
7. 進捗: `総文字数 / 目標` の達成率と、締切までの残り日数（締切が過去の作品は日数を表示せず達成率のみ）。
8. 最新行が想定より古い場合（自動記録の遅延）は、最新の2行で比較し、日付を明示する。行が無い場合は該当行を静かに省略する（失敗扱いにしない）。

### 4.3 朝報告での使い方（§5 `01-today-task`）

08:00 の時点で最新の記録行は前日 09:45 のスナップショットになるため、「昨日の実績」= 前日の行 − その前日の行 とする。

```
📊 昨日の執筆実績
・合計 +1,850文字
・港町の人魚 +1,850（累計 36,065 / 目標 30,000・達成 120%）
```

## 5. チャンネル別仕様

チャンネル名は `.env` の `CHANNEL_*` で上書き可。ルーティングはチャンネル名で行う。Bot が書き込む Obsidian のパスはすべて §3.1 の配下で、§2 のガードを通る。

### 01_DAILY & HEALTH

**`01-today-task`**
- 08:00 に自動投稿（`generate_morning_report()`）。前処理として §6 のタスク同期を実行してから:
  1. 前日の成果: 執筆実績（§4.3）、前日の体調スコア・ご機嫌度
  2. 本日のメインタスク（最大3件）とサブタスク（無制限）を番号付きで表示
  3. 「未完了タスクは3日後にバックログへ移ります」の一文
- 選出ルール: 実行予定日が今日以前の未完了タスク。優先度「高」→ 期限が近い順 → 登録順で並べ、先頭3件をメインとする
- ユーザー入力: 「完了 1,3」で完了、それ以外の文は「今日絶対やる緊急タスク」として本日メインに追加（4件目以降はサブ）
- 自動クリーン: 実行予定日から3日以上経った未完了タスクを、通知・叱責なしで実行予定日を空にしてバックログへ戻す

**`02-health`**
- 入力は2種類: ① Garmin/ヘルスケアのスクショ（Vision で 睡眠時間・深い睡眠・歩数・ストレス・心拍を抽出）、② 主観メモ（短文）
- どちらもタイムスタンプ付きで `02-health` シートへ。AIが判定（🟢/🟡/🔴）と体調スコアを付与し、短く返信
- 分析: 蓄積データをクロス集計し「自分取扱マニュアル」（睡眠と気分の相関、警戒アラート、更新提案）を `04-report` で更新し、`06-Life-OS/02-health/Health-Manual.md` に保存。`02-health` で「取扱マニュアル」と発言したときも生成

**`03-looking-back`**
- 21:00 に天気（Open-Meteo、地点は `WEATHER_LAT/LON`）付きで問いかけ
- ユーザー入力: ご機嫌度（1〜5）＋自由文。AIが整形 → `03-looking-back` シートと `06-Life-OS/03-looking-back/YYYY-MM-DD.md` へ保存
- 「X案」「note案」と書けばポスト案・noteネタを生成（頼まれたときのみ）
- 保存後、タスク棚（§6 で同期済みのバックログ。優先度・期限順）から候補を番号付きで提示。`1, 3` のような返信で翌日の実行予定日を設定（翌朝の `01-today-task` に反映）。「なし」「0」で終了
- 状態: 提示メッセージ ID に候補リストを紐付け（そのメッセージへの返信、またはチャンネル内の最新提示に対する番号入力）
- 日記本文から抽出するタスクは、明示的な「明日〜する」表現のみで、バックログに追加

**`04-report`**（投稿専用）
- 週次: 日曜 20:00 / 月次: 月末日 20:00
- 内容:
  1. 体調・感情の相関（ご機嫌度平均、条件別傾向）
  2. お金（娯楽費累計と先月比、事業損益、未転記件数）
  3. 執筆・タスク・プロジェクト進捗（週間/月間の増加文字数、作品別の達成率と締切までの日数、タスク消化数）
  4. スクラップ再浮上（進行中プロジェクトに関連する過去記事を1本）
- `04-report` シートと `06-Life-OS/04-report/` に保存

### 02_LIFE & MONEY

**`05-private`**: 独り言メモ。日付付きで `05-private` シートと `06-Life-OS/05-private/Inbox.md` に静かに記録（リアクション📝のみ）。「行きたいカフェ教えて」等の問い合わせのときだけ過去メモから回答。

**`06-household-accounts`**: 手入力は「現金」と「娯楽」のみ。`現金 カフェ 800円` / `娯楽 ガチャ 3,000円` をAIで構造化して `06-household-accounts` シートへ。返信に今月の娯楽費累計を必ず添える（数字を淡々と示す。責めない）。月末の楽天家計簿スクショは画像として受け取り保存（金額抽出はしない）。

**`07-ledger`**: 短文メモ or レシート写真（Vision）→ `07-ledger` シートへ。`10/5 サーバー代 1,200円` / `10/10 BOOTH売上 50,000円`。転記チェック2列は「10月分転記した」と発言、またはシート上で直接チェック（「やよい」「分析」の語で片方のみ、無ければ両方）。返信に今月概算損益と未転記件数を添える。

**`08-scrap`**: URL投稿のみ（`#タグ` 併記可）。本文取得 → 3行要約・タグ（3〜5個、投稿時指定があればそれを優先）→ `08-scrap` シート＋ `06-Life-OS/08-scrap/<日付>_<タイトル>.md`（先頭にメタデータ、本文全文、アイキャッチ1枚のみ）。Botの返信へのリプライで「#イラスト を追加」「タグを #設定 に変更」を実行（両方の保存先を更新）。

### 03_CREATIVE & LAB

**`09-idea`**（**実装済み**）: 完全サイレント（質問・提案・返信なし。リアクションだけ）。`06-Life-OS/09-idea/Ideas.md` の見出し直下に、`## YYYY-MM-DD HH:MM` ＋タグ行＋本文を挿入（最新が上）。`09-idea` シートにも記録（日時 / タグ / 内容）。
- **タグ**: AI が `settings.py` の `idea_tags`（既定: `#小説 #TRPG #仕事 #日常 #開発`）から1〜3個を自動で付ける。候補外のタグは付けない（どれも合わなければ `#日常`）。本文に自分で書いた `#タグ` はそのまま生かし、AI のタグより先に並べる。AI が使えなくても、既定タグで保存する。
- **タスク連携**: 「〜を買う」「〜に連絡する」のように、ユーザー自身がやる具体的な行動が書かれていれば、バックログ（実行予定日なし・出典 `09-idea`）に追加し、タスク棚（Obsidian）にも同期する。創作のアイデア・感想・迷いはタスクにしない。
- **画像**: Discord の添付 URL は数時間で失効するため、画像そのものを `06-Life-OS/09-idea/attachments/YYYYMMDD-HHMMSS-N.拡張子` に保存し（GAS 中継で新規作成）、`![[名前]]` で埋め込む。5MB を超える画像・画像以外のファイルは保存せず、名前と理由だけをメモに残す。画像だけの投稿は AI を呼ばず、`#日常` で保存する。画像の保存に失敗しても、本文は残す。
- **リアクション**: 📝 保存した / 📋 タスクにも連携した / ⚠️ 保存はしたが、シートかタスクへの連携に失敗した。Obsidian への保存に失敗したときだけ、共通のエラーメッセージを返す（何も記録していないことを知らせるため）。
- **順序と安全**: Obsidian を先に書き、失敗したらシートにもタスクにも記録しない。本文の行頭の `# ` `## ` は、`Ideas.md` の見出し構造と混ざらないようエスケープする。同時に投稿されても更新が失われないよう、`Ideas.md` の書き換えは1件ずつ行う。書き込みは `06-Life-OS/09-idea/` の中だけ。

**`10-project-novel` / `11-project-trpg` / `12-project-others`**: 作品ごとのノートを各部屋フォルダに `Novel_<作品名>.md` / `TRPG_<作品名>.md` / `Project_<名前>.md` として作成する。
- 投稿を種別分類（進捗・次やる・後回し・アイデア・詰まり・検索・相談）して追記、**部屋ごとの別シート**（`10-project-novel` / `11-project-trpg` / `12-project-others`。作品名は列で持つ）にも記録
- 「後回し/次やる」はタスクに連動（次やる=優先「高」、夜の候補に提示）
- 作品名未指定の初回は `Novel_未命名_YYYYMMDD.md`。「タイトル『〇〇』に決まった」でファイル名・ノート内タイトルを改名
- 検索・相談（「どう思う？」「スケジュール相談」「前に言ってた〇〇」）にだけ応答。それ以外は📝リアクションのみ
- 部屋に複数作品がある場合は投稿文から作品を判別し、曖昧なら直近に触った作品
- 進捗の報告（「今日1,000文字書いた」）は自己申告として記録するだけで、文字数の正は執筆記録シート（§4）
- 作品名は執筆記録シートの正規名（§4.2-1）と照合できるよう、ノート名に同じ表記を使う（不一致時はAIで近い作品を推定し、確信がなければ何も紐付けない）
- スケジュール相談では、執筆記録シートの目標・締切・実績（§4）を材料にする

### 04_CEO & SYSTEM

**`13-im-the-ceo`**: 方針・宣言を `06-Life-OS/13-im-the-ceo/CEO-Directives.md` と `13-im-the-ceo` シートに追記。**全AI呼び出しのシステムプロンプトに最優先で注入**（キャッシュし、更新時に再読込）。

**`14-ai`**: 総合秘書。Claude `tool_use` のループで以下ツールを自律実行:
`search_sheet` / `read_sheet_rows` / `append_row` / `update_row` / `delete_row` / `search_notes` / `read_note` / `append_note` / `read_writing_progress`（執筆記録シートの参照）。
- Obsidian 系ツールは §2 のガードを必ず通る。`search_notes` / `read_note` は Vault 全体を読めるが、`append_note` など書き込み系は許可領域のみ。許可外への書き込みを指定されても実行しない
- 変更・削除は実行前に `14-ai` シート（操作ログ）へ変更前の値を保存（取り消し可能にするため）
- 執筆記録シートには書き込まない（読み取りツールのみ）
- 曖昧な削除対象（複数候補）のみ確認を返す。それ以外は即実行して結果を報告
- 「システムのカスタマイズ」（要約を短く等）は `data/settings.json` の実行時設定を書き換えて反映。コードそのものは変更しない
- 雑談・相談も可（全肯定スタンス）

## 6. タスク棚の双方向同期（Obsidian ⇄ スプレッドシート）

対象ファイルは `01🗃Task/01 Life-OS-Task.md` のみ（§2）。Obsidian Tasks プラグインの記法で読み書きする。

### 6.1 記法と対応

| Tasks プラグイン | スプレッドシート（`01-today-task` シート） |
|---|---|
| `- [ ]` / `- [x]` | 完了（空 / `TRUE`） |
| 説明文 | 内容 |
| `⏳ YYYY-MM-DD`（scheduled） | 実行予定日（Bot が管理する「やる日」） |
| `📅 YYYY-MM-DD`（due） | 期限（ユーザー管理。Bot は変更しない。候補の並べ替えに使用） |
| `⏫` / `🔼` / `🔽` | 優先度 高 / 中 / 低 |
| `🆔 lo-xxxxxx` | ID |
| `✅ YYYY-MM-DD` | 完了日（完了時に Bot が付与） |

- Bot が出力する行は Tasks プラグインの標準的な順序（説明、🆔、優先度、➕、⏳、📅、✅）にする。読み取りは順序不問。
- `🆔` が無い行（ユーザーが手で書いた新規タスク）は、初回の同期で Bot が `🆔` を付与して書き戻す（許可ファイル内なので可）。

### 6.2 同期ルール

1. 頻度: 10分ごと。ほかに 08:00 朝報告の前、21:00 の候補提示の前、Bot 側でタスクを変更した直後（書き込み）に実行する。
2. スナップショット: 前回同期時の各タスクの内容ハッシュを SQLite（`task_sync`）に保存し、どちら側が変更されたかを判定する。
3. 片側のみ変更: そのまま反映する。
4. 両側が変更: **Obsidian を優先**する（手で編集した内容を尊重）。ただし「完了」はどちらかが完了なら完了とする（完了は取り消さない）。
5. Bot 側で新規追加されたタスク（`01-today-task`、`09-idea`、プロジェクト部屋、`03-looking-back` 由来）は、ファイル末尾の `## 🤖 Life-OS` 見出しの下に追記する。既存の見出し構造や順序は変更しない。見出しが無ければ末尾に作成する。
6. Obsidian 側で行が削除された場合、シートの `完了` に `削除` を入れて一覧から除外する（シートの行は消さない）。
7. 完了して30日を超えた行は同期対象から外す（行は消さない）。
8. 既存行の編集は該当行だけ差し替える。ファイル全体を書き直す場合も、その他の行の内容・空行・インデントは変えない。
9. Drive 使用時は、読み込み時の更新時刻を保存し、書き込み前に変わっていたら再読込して再計算する（Obsidian 側での同時編集との競合を避ける）。
10. 同期に失敗した場合は Bot 側の処理を止めず、次回に持ち越す。ユーザーには通知しない（ログのみ）。

## 7. 横断機能

- **リプライ/番号選択の状態保持**: `state.py`（SQLite）。`pending(message_id, channel_id, kind, payload_json, created_at)`。30日で自動削除
- **スケジューラ**（`discord.ext.tasks`、JST）: 08:00 朝報告、21:00 振り返り、日曜20:00 週次、月末20:00 月次、10分ごとのタスク同期。実行済み判定を状態ストアに持ち、二重投稿を防ぐ
- **多重起動の防止**（マニュアル 8-11 の発展版。`instance_guard.py`）: Bot が二重に動くと、朝の通知や返信が重複するため、2段構えで防ぐ。
  1. **同じパソコン内**: OS のファイルロック（`data/bot.lock`）。2つ目は「すでにこのパソコンで Bot が起動しています（PID …）」と表示して終了する（終了コード1）。プロセスが異常終了しても OS がロックを自動で解除するので、古い PID ファイルが残って起動できなくなることがない。
  2. **パソコンと VM など別の場所どうし**: `06-Life-OS/14-ai` フォルダのメタデータ（Drive の `appProperties`。ファイルの中身は変わらず、Obsidian には見えない）に、期限つきの「使用中の印」（`lease_id` `lease_host` `lease_ts`）を書く。起動時に、他が使用中（90秒以内に更新）なら「すでに別の場所（ホスト名）で Bot が動いています」と表示して終了する。起動後は30秒ごとに更新し、期限は90秒。ほぼ同時に起動した場合は、3秒待って読み直し、後から書いた方だけが残る。
  3. **停止時**: 正常に止まると、自分の印だけを消す（他の Bot の印は消さない）。クラッシュした場合は、約90秒で自動的に解除される。別の Bot に引き継がれた、または30秒×3回近く更新できない状態が続いた場合は、二重になるのを避けるため、自分から停止する（終了コード2）。
  4. **systemd の自動再起動への対応**: 同じホスト名の印が残っている場合（前回この場所で動いていた Bot が異常終了した跡）は、`ProcessLock` を取得済みのときに限り、期限（90秒）を待たずに引き継ぐ。別のホストの印は、期限が切れるまで引き継がない。終了コードは、すでに別の Bot が動いているとき **3**、印を他に引き継がれて停止したとき **2**（systemd は `RestartPreventExitStatus=3` で、3 のときは再起動を繰り返さない）。
  5. **朝・夜の投稿の重複防止**: 定時の投稿の前に、Discord のチャンネル履歴（直近30件）を確認し、同じ日に Bot が同じ書き出し（朝: 「おはようございます！ 本日のタスク案内」、夜: 「今日もお疲れ様でした！ 本日の記録を残しましょう」）で投稿していれば投稿しない。パソコンと VM を切り替えた日に、片方が先に投稿していても重複しない（`state.py` の実行済み記録は各マシン内だけのため）。履歴を読めなかったときは投稿する。
  6. **運用**: パソコンから VM に切り替えるときは、パソコンの Bot を停止（Ctrl+C）してから VM で起動する。印の書き込みは書き込み許可領域（§2）の中で、通常のガードを通る。開発用の `local` バックエンドでは、印は `data/local_props.json` に保存される。
- **音声入力**: Discordの音声メッセージは対象外（スマホの音声入力によるテキストを想定）
- **エラー処理**: 失敗時は「うまく記録できませんでした。もう一度送ってください（原因: …）」と短く返し、例外はログに出す。ユーザーを責めない文面
- **Claude呼び出し**: `claude_client.py` に集約（`complete()`, `complete_json()`, `vision_json()`, `run_tools()`）。JSON解析失敗時はフォールバック値を返す

## 8. ファイル構成と設定

```
life-os-bot/
  main.py              # Botクライアント、イベント、ルーティング
  config.py            # 環境変数、チャンネル名、既定値
  scheduler.py         # 定時ジョブ
  claude_client.py     # Claude API ラッパ、人格プロンプト、CEO宣言注入
  sheets.py            # gspread ラッパ（Bot用ブックのスキーマ、CRUD）
  writing_log.py       # 執筆記録シートの読み取り（§4）
  notes.py             # NoteStore（local / drive）。全操作で notes_policy を通す
  notes_policy.py      # 許可リストと assert_allowed（§2）
  task_sync.py         # Obsidian ⇄ スプレッドシートのタスク同期（§6）
  state.py             # SQLite 状態ストア
  settings.py          # 実行時設定
  weather.py           # 天気取得
  handlers/            # チャンネル別ハンドラ
  tests/               # 純粋ロジックの pytest（notes_policy、執筆記録パース、タスク同期、番号解析ほか）
  requirements.txt / .env.example / .gitignore / README.md
```

主な環境変数: `DISCORD_TOKEN` / `ANTHROPIC_API_KEY` / `GOOGLE_SHEET_ID` / `CREDENTIALS_FILE`（サービスアカウント鍵。既定 `credentials.json`）/ `WORDCOUNT_SHEET_ID` / `WORDCOUNT_TAB`（既定 `シート1`）/ `OBSIDIAN_BACKEND`（drive|local。local は開発用）/ `OBSIDIAN_LIFEOS_FOLDER_ID` / `OBSIDIAN_TASKFILE_ID` / `OBSIDIAN_VAULT_FOLDER_ID`（許可外ノートの読み取り用。任意）/ `OBSIDIAN_VAULT_DIR`（local のみ）/ `OBSIDIAN_TASK_FILE`（既定 `01🗃Task/01 Life-OS-Task.md`。空白まで完全一致で判定）/ `GAS_RELAY_URL` `GAS_RELAY_TOKEN`（GAS 中継。トークンは秘密情報）/ `USE_TRUSTSTORE`（開発時のみ 1）/ `CHANNEL_*` / `WEATHER_LAT` `WEATHER_LON`

追加ファイル: `deploy/`（`life-os-bot.service` `setup_vm.sh` `update.sh`）、`docs/DEPLOY.md`（24時間稼働の手順書）、`scripts/make_vm_env.py`（VM 用の `.env` の生成）、`instance_guard.py`、`.gitattributes`（`.sh` `.service` `.py` は LF）、`vault_paths.py`、`gas_relay.py`、`gas/`（`Code.gs` `appsscript.json` `README.md`）、`scripts/verify_relay.py`、`requirements-dev.txt`（`truststore` `pytest`）（許可領域内のノート配置の一元管理）。

### 8.1 実装の現状（フェーズ 0・0.5・1）

ユーザーの指示（セキュリティ設定・権限設定・SPEC.md の更新）に従い、次の範囲だけを実装済み。機能ハンドラ（各チャンネルの処理、スケジューラ、タスク同期、執筆記録の読み取り）は**未実装**で、着手の指示を待つ。

| 状態 | ファイル | 内容 |
|---|---|---|
| 実装・検証済み | `notes_policy.py` | 読み取りは Vault 全体で許可、書き込み系は許可リストのみ。拒否時に `data/guard.log` へ ERROR を記録し `AccessDenied` を送出 |
| 実装・検証済み | `notes.py` の `GuardedStore` / `LocalStore` | 全操作がポリシーを通る。削除はゴミ箱退避。検索範囲は `06-Life-OS`（既定）か Vault 全体 |
| 実装・検証済み | `notes.py` の `DriveStore` | サービスアカウント認証のみ。**サービスアカウントは何も作成しない**（読み取りと既存ファイルの更新だけ）。新規ファイル・画像・改名・ゴミ箱移動は `gas_relay` に依頼。実機で、読み取り・既存ファイルの更新・許可外の拒否・中継未設定時のエラーを確認済み。偽の Drive と偽の中継のテストで、更新は中継を呼ばず新規は中継のみ、許可外は両方に届かないことを検証 |
| 実装・実機検証済み | `gas_relay.py` | 中継クライアント。`create_file` `create_bytes` `rename` `trash` の4操作のみ。同じ `request_id` で最大2回再試行、GAS の 302 リダイレクトに追従、`denied` は `AccessDenied` として `data/guard.log` に記録、トークンはログ・例外に出さない。偽サーバーでテスト済み |
| 実装・実機検証済み（2026-09-19 デプロイ済み） | `gas/Code.gs` `gas/appsscript.json` `gas/README.md` `scripts/verify_relay.py` | 中継本体（許可領域 `06-Life-OS/` の再検証、トークンの定数時間比較、`LockService` による直列化、`request_id` の再送検出、既存ファイルの非上書き）、デプロイ手順書、12項目の実機検証スクリプト。GAS 側は自動テスト環境（Node 等）が無いため、`verify_relay.py` で実機検証した（下記）。GAS エディタの `selfTest` も「すべて成功」（ユーザー報告） |
| 実装済み（VM での実機検証は待ち） | `deploy/`（systemd の設定・セットアップ・更新スクリプト）`docs/DEPLOY.md` `scripts/make_vm_env.py` | マニュアル第8章（9-1〜9-6）の私専用版。Ubuntu 22.04/24.04 向け。venv に依存を入れる（`--break-system-packages` は使わない）。`Restart=on-failure`、終了コード3では再起動しない。`.env` と `credentials.json` は GitHub に上げず、SSH 画面から VM に置く（`credentials.json` はファイルのまま。マニュアルの `GOOGLE_CREDENTIALS_JSON` は使わない）。`git init` 済み（コミット・push はユーザーが行う）。GitHub に上がるファイル（46個）に、`.env` の秘密の値・秘密鍵の形式は含まれないことを確認済み（`.env` `credentials.json` `deploy/vm.env` `data/` 執筆記録 個人の設計資料・購入したマニュアルは `.gitignore` で除外）。全 `.py` は Python 3.10 の文法で検査済み。シェルスクリプトは `bash -n` で構文確認済み |
| 実装・実機検証済み | `instance_guard.py`（`main.py` に組み込み済み）`notes.py` の `get_props`/`set_props` | 多重起動の防止（§7）。実機で確認: ①同じパソコンで2つ目を起動 → 拒否（PID 表示・終了コード1）②別のパソコン（VM）の想定で使用中の印を取りに行く → 拒否、印は元の Bot のまま ③Bot を強制終了した直後に起動 → 印が残っているので拒否（約90秒で解除）④期限切れの印（200秒前）→ 引き継いで起動 ⑤正常終了後は印が消える。プロセスロックは、実際に別の Python プロセスを起動するテストで検証（保持中はブロック、強制終了後は起動できる） |
| 実装・検証済み | `tests/`（`test_notes_policy.py` `test_util.py` `test_gas_relay.py` `test_drive_store_relay.py` `test_task_sync.py` `test_writing_log.py` `test_phase1_pure.py` `test_instance_guard.py`） | 241件成功・1件スキップ（Windows でのシンボリックリンク作成）。書き込み制限・中継・タスク同期の判断（競合・削除・完了・ID付与・他の行の保持）・執筆記録の集計・見出しの判定・朝の案内・夜の選択・体調の集計 |
| 更新済み | `config.py` `.env.example` `.gitignore` `requirements.txt` `requirements-dev.txt` `CLAUDE.md` `main.py`（`USE_TRUSTSTORE=1` のときだけ `truststore` を有効化）`vault_paths.py`（実際のファイル名に修正）`claude_client.py`（CEO 方針の読み込み先を修正） | 3つの ID・タスク棚パス（`01🗃Task/…`）、GAS 中継の設定、OAuth 関連の削除、ガード方針の記載。`.env` を作成済み（下記） |
| 実装・実機検証済み（フェーズ 3 の1つ目: `09-idea`） | `handlers/idea.py`（`main.py` にルーティング済み）`vault_paths.idea_attachment` | §5 のとおり。テスト22件（サイレント・最新が上・タグの規則・タスク連携・画像の保存と失敗・Obsidian 失敗時に何も記録しない・シート/タスク失敗時の⚠️・同時投稿・書き込み先が `09-idea/` の中だけ）。ロックを外すと同時投稿のテストが失敗することを確認済み。実機（実際のシート・Drive・Claude・GAS 中継）8/8 成功: 3件の投稿で返信なし・最新が上・タグ自動付与・画像の中継保存と埋め込み・シートに3行・「牛乳を買う」だけがバックログとタスク棚に入る。検証データは元に戻した（`attachments/` の空フォルダだけが残る。フォルダはゴミ箱に移せないため） |
| 実装・実機検証済み（フェーズ 1） | `sheets.py` `writing_log.py` `task_sync.py` `health_analysis.py` `handlers/today_task.py` `handlers/looking_back.py` `handlers/health.py` `scheduler.py` `main.py` | シート名はチャンネル名（§3.2）。見出し行は列名で読み書きし、既存データのあるシートには書かない。`ensure_schema` で14シートに見出しを書き込み済み（`08-scrap` の A1 も置き換え済み）。朝 08:00・夜 21:00 の定時投稿（同じ日に二重投稿しない）、10分ごとのタスク同期。`main.py` は 01 / 02 / 03 だけをルーティングし、ほかのチャンネルは何もしない（記録したように見せない） |
| 下書き・一部のみ検証 | `util.py` `state.py` `settings.py` `weather.py` `claude_client.py` `vault_paths.py` | フェーズ 1 の実機検証の中で、使われている範囲は動作を確認済み。`settings.py` などは後続のフェーズで使うため未検証 |

**フェーズ 1 の実機検証結果（2026-09-19・実際のシート／Obsidian／Claude／GAS 中継を使用。Discord へは投稿せず、偽のメッセージで各ハンドラを呼んだ。検証データは終了時にすべて元へ戻した）15/15 成功**: ①執筆記録の読み取り（24作品・20記録行）と朝の執筆ブロック ②タスク同期: シートのタスクが Obsidian の `## 🤖 Life-OS` に出る／Obsidian で完了にするとシートも完了＋完了日／Obsidian に手書きした行に ID が付いてシートに追加／Obsidian で行を消すとシートは「削除」 ③朝の案内（番号つきタスク）・緊急タスクの追加・「完了 N」 ④日記（Obsidian に新規作成は中継、シートに記録）・バックログ候補の提示・番号選択で翌日の予定日を設定・「明日〜する」のバックログ追加 ⑤体調の記録と判定・取扱マニュアルの生成（`Health-Manual.md` は元の内容に復元）。後片付け後の状態: タスク棚は空、日記ファイルは残っていない、3つのシートはデータ行 0。Discord へのログインと定時ジョブの登録も確認済み（次回の朝の投稿は 2026-09-20 08:00、夜は 21:00）。

**GAS 中継の実機検証結果（`scripts/verify_relay.py`、12/12 成功）**: ①中継で新規作成→読み取り ②中継が作ったファイルをサービスアカウントが更新できる ③既存ファイルは上書きされない（`exists`） ④バイナリの作成 ⑤改名 ⑥ゴミ箱への移動 ⑦許可外パス、⑧`..` によるすり抜け、⑨`06-Life-OS` 直下（ルート）、⑩タスク棚は GAS 側で `denied` ⑪不正トークンは `unauthorized` ⑫Bot 側のガードは中継を呼ぶ前に拒否。検証後、`14-ai` フォルダは空で、`06-Life-OS` 直下にファイルは残っていない。

`.env`（秘密情報。Git 管理外）: `.env.example` から作成済み。自動で入力済み: 3つの Drive ID、`GAS_RELAY_TOKEN`（自動生成した43文字のランダム値）、`USE_TRUSTSTORE=1`。**ユーザーの入力待ち**: `DISCORD_TOKEN` `ANTHROPIC_API_KEY` `GOOGLE_SHEET_ID`（Bot 用スプレッドシート）`WORDCOUNT_SHEET_ID`（執筆記録シート）`GAS_RELAY_URL`（GAS のデプロイ後）。

**Python のバージョン差（VM の実機で発覚した不具合と対策）**: 開発用パソコンは Python 3.14、VM は 3.10〜3.12。3.14 は型ヒントの評価を後回しにするため、クラス内で `def list(...)` を定義したあとの `-> list[dict]` が、3.12 以前では組み込みの list ではなくメソッドを指し、`notes.py` の読み込み時に `TypeError: 'function' object is not subscriptable` になる不具合が、開発用パソコンでは隠れていた（VM で `verify_relay.py` を実行して発覚）。対策: ①`notes.py` に `from __future__ import annotations` を追加 ②`tests/test_py_compat.py` を追加（クラス内の名前が型ヒントを隠す箇所と、Python 3.10 で使えない構文を、どの Python でも自動で検出する。修正前の `notes.py` で、`DriveStore.search`（370行目）と `GuardedStore.search` を検出できることを確認済み）③Python 3.12 でも、全モジュールの読み込みとテストを確認（198件成功・1件スキップ。`tests/test_drive_store_relay.py` の17件は、このパソコンの制限（アプリケーション制御ポリシーが 3.12 の `_ssl` をブロック）で 3.12 では実行できず、3.14 で成功）。今後、機能を追加するときは、3.14 だけでなく古い Python でも壊れないことを、このテストで確認する。

開発環境: `life-os-bot/.venv`（Python 3.14.7）に依存ライブラリを導入済み。**開発用パソコンでは Google への通信で SSL 証明書エラー（`CERTIFICATE_VERIFY_FAILED`）が出る**（Python 同梱の証明書束では検証できず、Windows の証明書ストアなら成功する。パソコン側のセキュリティソフトによる通信検査が原因の可能性がある）。**方針（ユーザー承認済み）**: 証明書検証は**無効にしない**。開発時のみ `truststore`（OS の証明書ストアを使うライブラリ）を使い、起動時に `truststore.inject_into_ssl()` を呼ぶ。`truststore` は開発用の依存（`requirements-dev.txt`）に置き、GCP VM の本番依存には含めない。組み込み方: `main.py` の先頭で環境変数 `USE_TRUSTSTORE=1` のときだけ `truststore.inject_into_ssl()` を呼ぶ（既定は呼ばない）。組み込みはユーザーが了承済みで、実装の指示に合わせて行う（現時点では venv に導入しただけで、アプリのコードには未組み込み）。

## 9. 実装フェーズ（順次作業）

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| 0（進行中） | 基盤の整備（**セキュリティ部分は完了**: ガード・テスト・CLAUDE.md。Drive 実機検証は完了）: 依存ライブラリ検証、`notes_policy.py`（書き込み制限・読み取り許可）とテスト、`notes.py` 修正、`writing_log.py`、`sheets.py` 修正、main骨格、`CLAUDE.md` への制約反映 | 全チャンネルに📝で応答。許可外パスの拒否テストが通る |
| 0.5（**完了**: 実機検証 12/12 成功） | **GAS 中継**（§2.5）: `gas/Code.gs`、`gas_relay.py`、`DriveStore` の新規作成・改名・削除・フォルダ作成、偽中継のテスト、`verify_relay.py`、`truststore` の組み込み。**デプロイはユーザー作業** | 実機で「新規作成→サービスアカウントで更新→改名→ゴミ箱」が通り、許可外・不正トークンが拒否される |
| 1（**完了**: 実機検証 15/15 成功） | 毎日: 01 / 03 / 02、朝・夜スケジュール、`task_sync.py` | タスク→夜選択→翌朝表示が一巡。Obsidian のタスクとシートが同期 |
| 2 | お金・私生活: 05 / 06 / 07 | 各入力が構造化されて記録 |
| 3 | 知識・創作: 08 / 09 / 10-12 | Obsidianへ追記・改名・タグ編集 |
| 4 | CEO/秘書: 13 / 14 | ツール実行と操作ログ |
| 5 | レポート04、README（GCP常時起動手順、systemd） | 週次・月次生成 |

## 10. 決定事項と確認したい事項

### 決定済み（ユーザー回答）

1. 文字数管理シートの構造 → `執筆記録.xlsx`（Google スプレッドシート版と同構造）。§4 に反映済み
2. Obsidian のタスク棚との双方向同期 → 行う（§6）
3. Obsidian の許可範囲 → §2 のとおり。書き込み・変更・新規作成は `06-Life-OS/` 配下と `01🗃Task/01 Life-OS-Task.md` のみ。許可外は拒否・エラーログ記録・安全に中断
4. 開発環境の Python → 導入済み（3.14.7）
5. Obsidian の読み取り → **Vault 全体で許可**（書き込み系のみ許可リストで制限。§2）
6. Bot の稼働場所 → GCP VM で24時間常時稼働。Obsidian は Drive API v3（`drive` 方式）。認証は**サービスアカウント（credentials.json）のみ**。OAuth は使わない（§2.4）
7. `01 Life-OS-Task.md` → `.md`、Tasks プラグイン記法。現在は白紙（§6 の同期で `## 🤖 Life-OS` 見出しを作って追記する）
8. 執筆記録シート → サービスアカウントを閲覧者として共有済み。Obsidian の Drive フォルダと Bot 用スプレッドシートはサービスアカウントに編集者として共有して運用する
9. フォルダ名 → `01🗃Task`（空白なし）。タスク棚は `01🗃Task/01 Life-OS-Task.md`
10. Drive の ID → Vault ルート・`06-Life-OS` フォルダ・`01 Life-OS-Task.md` の3つを受領（§2.4）
11. 権限設定 → 完了。Vault ルート=閲覧者、`06-Life-OS` とタスク棚=編集者（サービスアカウント）
12. 実装の範囲 → セキュリティ設定・権限設定・SPEC.md の更新（実施済み）。機能ハンドラの実装は別途指示を待つ
13. 開発時の SSL 対応 → 証明書検証は無効にせず、開発時のみ `truststore` を使う（§8.1）
14. Google Drive API → 有効化済み
15. 朝報告の作品 → 直近14日に増加した作品のみ（§4.2-6）
16. 新規ファイルの作成方法 → **B（GAS 中継方式）を採用**。日次日記・記事別スクラップ・作品別ノートの自動生成を維持する（§2.5）
17. 検証用ファイル → `Ideas.md` `Inbox.md` `CEO-Directives.md` `Health-Manual.md` の4つを作成済み。実機で更新に成功（§2.4）。ファイル名は実物に合わせる
18. SSL 対応 → `truststore` の組み込みを了承（実装の指示に合わせて組み込む。§8.1）
19. フェーズ 0.5（GAS 中継）の実装 → 指示済み・実装済み（Bot 側とテスト、GAS のコードと手順書）。GAS のデプロイはユーザー作業
20. 準備状況 → Discord（14チャンネル・BOT トークン・メッセージ内容の読み取り権限・招待）、Anthropic の API キー、Bot 用スプレッドシートの作成とサービスアカウントへの共有（編集者）は完了と報告済み。値は `.env` への入力待ち
21. シート名 → **チャンネル名と同じにする**。ユーザー作成済みの14個のシートをそのまま使う。`10-project-novel` `11-project-trpg` `12-project-others` は別々のシート（あとで確認しやすいように）（§3.2）
22. シートの見出し → 既存の14シートに書き込み済み。`08-scrap` の A1 は見出しに置き換え済み（ユーザーが文字を削除し、見出し書き込みを許可）
23. フェーズ 1 の実装 → 指示済み・完了（§8.1）
25. 24時間稼働（マニュアル第8章）→ 手順書と設定ファイルを用意（`docs/DEPLOY.md`）。GitHub のリポジトリ作成・push・GCP の VM 作成・ファイルのアップロードはユーザーの作業。`.env` と `credentials.json` は GitHub に上げず、VM に直接置く
24. 多重起動の防止（マニュアル 8-11）→ 指示済み・完了。パソコンと VM の両方で Bot が動いて通知が重複することも防ぐ（§7）
26. フェーズ 3 の進め方 → `09-idea` から実装（指示済み・完了）。続いて `08-scrap`、`10〜12`

### 確認したい事項（既定の仮定つき。異論がなければ仮定で進める）

1. **【要対応】24時間稼働の作業**（`docs/DEPLOY.md`。手順 1〜7）。VM での実機検証（`check_connections.py`・`verify_relay.py`・パソコンを閉じた状態での返信・翌朝 8:00 の投稿）は、ユーザーが VM を作ってから確認する
2. **【要指示】次に進める機能**: フェーズ 3 の続き（`08-scrap`、`10〜12` のプロジェクト）、フェーズ 2（`05-private` `06-household-accounts` `07-ledger`）、フェーズ 3（`08-scrap` `09-idea` `10〜12` プロジェクト）、フェーズ 4（`13-im-the-ceo` `14-ai`）、フェーズ 5（`04-report` 週次・月次レポート、GCP VM での常時稼働）。ご指示をいただいてから始める
3. **Bot の起動**: 今は Bot は動いていない（検証のために短時間起動しただけ）。手元のパソコンで動かす場合は `.venv/Scripts/python main.py`（パソコンを起動している間だけ動く。2つ目を起動しようとすると拒否される）。24時間動かすにはフェーズ 5 の GCP VM が必要。VM で動かすときは、パソコンの Bot を先に停止する
4. 執筆記録シートに、作品名が「2026-09」という列がある（B 列）。朝の案内では「2026-09 +1,471」と表示される。作品名としてよいか（正規名は先頭の `YYYY-MM ` を除いたもの。`2026-09` のように接頭辞だけの名前はそのまま表示する）
5. 許可外ノートを読んだ内容は、ユーザーが依頼した検索・相談のときだけ Claude API に送る（定期処理では読まない。§2.4）。この運用でよいか
6. 起動が定時（08:00 / 21:00）に間に合わなかった日は、後追いの投稿をしない（思いがけない時間に投稿しないため）。よいか
