# 今後の進め方（引き継ぎメモ）

最終更新: 2026-09-25。次に戻ってきたときは、このファイルを上から順に進める。

## 今の状態

- 実装・実機検証済み: 01-today-task / 02-health / 03-looking-back / 08-scrap / 09-idea / 10-project-novel / 11-project-trpg / 12-project-others、朝夕の定時投稿、タスク同期（Obsidian ⇄ シート）、多重起動の防止、GAS 中継、Drive の遅延・書き戻し対策。
- **実装済み・自動テストのみ（実機検証は未実施）**: 05-private（`handlers/private.py`）、06-household-accounts（`handlers/household.py`）、07-ledger（`handlers/ledger.py`）。実機検証は、固有の目印つきの投稿で確認し、終わったらノート・シートの行を元に戻す。
  - 05: 保存・最新が上・タグ・シートの行・過去メモへの返信。
  - 06: `娯楽 テスト 1円` のような目印つきの投稿で、シートの行・返信の累計・複数行・「昨日 現金 …」・読み取れない文章のヒント・画像の保存（`attachments/`）。確認後、シートの行と画像を手で消す。
  - 07: 目印つきの `サーバー代テスト 1円`（経費）と `売上テスト 2円`、種別が決められない行（`ドメイン 3円`）が聞き返されること、**本物のレシート写真1枚**（Vision が日付・金額を読み、画像が `07-ledger/attachments/` に保存されてシートの内容に `[レシート: …]` が入る）、`X月分転記した`（**注意**: 実際の帳簿の行が既にあると、その月の行が本当に「転記済み」になる。先にテスト用の月を使うか、確認後にシートで `TRUE` を外す）。確認後、シートの行と画像を手で消す。
- **実装済み・自動テストのみ（実機検証は未実施）**: 04-report（`report.py`、`scheduler.py` の `report_job`）。日曜 20:00 に週次、月末日 20:00 に月次を `04-report` に投稿し、`06-Life-OS/04-report/Weekly_*.md` `Monthly_*.md` と `04-report` シートに保存する。
  - 先に **`scripts/preview_report.py weekly`**（投稿も保存もしない）を実行して、本物のデータで中身を見る: `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 .venv/Scripts/python scripts/preview_report.py weekly`（月次は `monthly`。日付を付けると、その日に発行したものとして作る）。
  - 投稿・保存までの確認は、VM の反映後の最初の日曜 20:00（または月末日 20:00）を待つ。すぐ確かめたいときは、`REPORT_TIME` を近い時刻にして、日曜（または月末日）に Bot を起動する。確認後、投稿とノート・シートの行は手で消してよい。
  - 注意: 実機で使うデータが少ないうちは、条件別の傾向（睡眠・雨）は出ない（条件ごとに 2 日以上が必要）。
- **実装済み・自動テストのみ（実機検証は未実施）**: 13-im-the-ceo（`handlers/ceo.py`）と 14-ai（`handlers/ai.py`、`ai_tools.py`）。これで 14 チャンネルすべてに実装が入った。実機検証は、必ず**目印つきのテスト用データ**で行い、終わったら元に戻す（14-ai はシートを実際に書き換える・削除する）。
  - 13: `テスト方針 目印123` と投稿 → 👑 が付く、`CEO-Directives.md` の先頭に入る、`13-im-the-ceo` シートに 1 行入る。その直後に、ほかの部屋（例: 14-ai）で「今の方針を教えて」と聞くと、その方針を踏まえて答える。確認後、ファイルの先頭の 1 件とシートの行を手で消す（消さないと、以降の全ての Claude への最優先の指示に入ったまま）。
  - 14（読み取り）: 「先月の事業の利益は？」「〇〇についてのノートはどこ？」など。何も変更されないことを確認する。
  - 14（変更）: **本物のデータには触らず**、`05-private` に `テスト 目印123` を投稿しておき、「05-private の『テスト 目印123』の行を消して」→ 返信の末尾に 🗂️ 操作ログが出る、`14-ai` シートに記録が入る、行が消える → 「さっきの取り消して」→ 行が戻る、を確認する。
  - 14（設定）: 「09-idea のタグに #テスト を追加して」→ 「さっきの取り消して」。
  - 14（安全）: 「13 の方針ノートに追記して」が断られる、「01-today-task のタスクを削除して」が Obsidian で行うよう案内される。
  - 注意: Sheets の読み取りは 1 分 60 回を全体で共有する。14-ai は 1 回の頼みで何度もシートを読むことがある。
- 自動テスト: 669 件成功・1 件スキップ（Python 3.14）。
- **未実装**: なし（14 チャンネルすべてに実装済み）。残りは、VM への反映と各機能の実機検証。
- **VM には、まだ反映していないものがある**（`08-scrap`・`10〜12` 以降の全部。下の手順 1）。

## 手順 1: VM に反映する（最初にやる）

1. パソコンで、変更をコミットして push する（コミットと push はユーザーが行う）。
   `git add -A` → `git commit -m "08-scrap と 10〜12 を追加"` → `git push`
   （`.env` `credentials.json` `data/` は `.gitignore` で除外済み。念のため `git status` で確認）
2. VM に SSH で入り、`bash deploy/update.sh` を実行する。
3. 手順は `docs/DEPLOY.md` を参照。パソコンの Bot が動いていないことを確認してから VM を起動する（多重起動の防止で、片方は終了コード 3 で止まる）。
4. Discord で確認する:
   - 08-scrap に記事の URL を投稿 → 📰 が付き、`06-Life-OS/08-scrap/` にノートができる。
   - 10-project-novel に「（作品名）2章まで書いた」と投稿 → 📝 が付き、`Novel_（作品名）.md` ができる。
   - 確認後、テストで作ったノートとシートの行は手で消してよい。
5. 問題があれば、VM で `journalctl -u life-os-bot -n 100` のログを見る。

## 手順 2: フェーズ 2（05・06・07）の実機検証

3 つとも実装済み。手順 1 の VM 反映のあとに、上の「今の状態」の手順で実機確認する。仕様は `SPEC.md` の §5（02_LIFE & MONEY）にある。

| 順 | チャンネル | 内容 | 注意点 |
|---|---|---|---|
| 1 | 05-private（実装済み・実機検証待ち） | 私生活の記録 | 手順 1 の VM 反映のあとに、実機で確認する |
| 2 | 06-household-accounts（実装済み・実機検証待ち） | 家計簿 | 金額の読み取り・月ごとの集計は、07 でも参考にできる |
| 3 | 07-ledger（実装済み・実機検証待ち） | 事業の帳簿（短文メモ・レシート写真） | 写真は Vision で読み取る（新しい部分）。転記チェック2列、今月の概算損益の返信 |

進め方（これまでと同じ）:
1. 着手前に、SPEC の該当箇所と入力例を確認する。
2. 実装 → 自動テスト（偽の Drive・偽の AI を使う）→ 実機検証（固有の目印つきデータで試し、終了後に元へ戻す）。
3. `SPEC.md` §8.1・§10 と `CLAUDE.md` を更新する。
4. push して、VM で `bash deploy/update.sh`。

## 手順 3: 04-report（週次・月次レポート）の実機確認

- 実装済み（上の「今の状態」）。VM に反映すれば、日曜 20:00 と月末日 20:00 に自動で投稿される。
- データが少ないうちは、レポートの中身は薄い。数週間使ってから見ると、傾向や先月比が出る。
- 出力先は `06-Life-OS/04-report/Weekly_*.md` `Monthly_*.md`（新規ファイルなので GAS 中継で作成する）。仕様は `SPEC.md` §5 の `04-report` の項。

## 手順 4: フェーズ 4（13-im-the-ceo / 14-ai）の実機確認

- 実装済み（上の「今の状態」）。設計上の判断は `SPEC.md` §5 の 14-ai の「仮定」に書いた（タスクの削除は Obsidian で行う・方針は 13 の投稿だけで変える・記憶は 3 往復 30 分・変更は確認なしで即実行するが、量の上限と取り消しで守る）。違っていたら伝える。
- `06-Life-OS/14-ai` フォルダは、多重起動の印（appProperties）に使っている。`14-ai` の `append_note` でこのフォルダにメモを作っても、印（フォルダのメタデータ）には影響しないはずだが、実機では未確認。作った直後に、Bot が「別の場所で動いています」と言い出さないか見る。

## 仮定のまま進めている点（違っていたら伝える）

- X（Twitter）の投稿は、第三者サービス fxtwitter 経由で取得する。
- 作品名がない初回は `未命名_日付` で作り、後で改名する。
- 相談・検索の質問そのものは、ノートに追記しない。
- 1 投稿の URL は 3 件まで。危険な URL は保存せず拒否する。

## 再開するときの注意（Claude 向け）

- `CLAUDE.md` と `SPEC.md`（特に §5・§8.1・§10）を先に読む。
- 仕様書の作成・改訂を頼まれたときは、SPEC.md だけを編集し、実装は明示の指示があるまでしない。
- 開発用パソコンは Python 3.14、VM は 3.10〜3.12。`from __future__ import annotations` と `tests/test_py_compat.py` を維持する。
- 実機テストは、Sheets の読み取り上限（1 分 60 回）と VM の Bot の同期にぶつかる。固有の目印を付け、元に戻し、Bot のプロセスを残さない。
- Drive の遅延・同期ソフトの書き戻しは `CLAUDE.md` の「Drive quirks」を参照。新しい保存処理は、必ず `DriveStore` を通す。
- 実機テストのスクリプトは、バックスラッシュを含む文字列を bash のヒアドキュメントで渡さず、ファイルとして書く（`\n` が壊れる）。

## 未解決の小さな項目

- **昨日の執筆実績は、朝の案内ではなく別の投稿**（`scheduler.post_writing`。`WRITING_TIME` 既定 10:00 以降、当日の行が入ってから1回）。昨日書いた文字数 = 当日の行 − 昨日の行（SPEC §4.3）。**実機では未確認**。VM に反映して、翌日 10:00 ごろに `01-today-task` に `📊 昨日の執筆実績` が出る（増えていない日は出ない）ことを確認する。VM の `.env` に `WRITING_TIME` は無くてよい（既定 10:00）。執筆記録シートの記録時刻を変えた場合は、`WRITING_TIME` をそれより後にする。
- **取扱マニュアルは、週次・月次レポートでも自動更新される**（`report.refresh_manual`。SPEC §5 の `02-health`）。実機では未確認。最初の日曜 20:00（または月末日）の後に、`Health-Manual.md` が更新され、`Health-Manual-前回.md`（前の版）が新規に作られ（GAS 中継）、`04-report` シートに 種別「取扱マニュアル」の行が入ることを確認する。手書きで書き足した内容は `Health-Manual-前回.md` に 1 世代だけ残る（次の更新で上書きされる）ので、残したい内容は別の場所に移す。
- `README.md` を作成した（概要・セットアップ・定時ジョブ・ファイルの案内）。機能が増えたら、チャンネルの表と定時ジョブの表を更新する。
- `09-idea/attachments/` の空フォルダが残っている（フォルダはゴミ箱に移せないため。害はない）。
- Python 3.12 では、`test_drive_store_relay.py` と `test_drive_recent.py` がこのパソコンの制限で実行できない（VM の 3.12 では実行できるはず）。
